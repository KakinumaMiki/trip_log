<h1 class="title"><%= @place.name %></h1>

<%= render 'prefectures/places/memories/memory_list', memory: @memory %>

<div class="pt-3">
  <%= link_to '場所の詳細に戻る', prefecture_place_path(@prefecture, @place), class: 'button is-info is-light' %>
</div>
